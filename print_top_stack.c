#include "monty.h"

/**
 * print_top_stack - print the top stack
 * @head: the head of the stack
 * @line_n: the line number in the function
 * Return: Return nothing
 */
void print_top_stack(stack_t **head, unsigned int line_n)
{
(void)head;
(void)line_n;
m.stackMode = 0;
}
