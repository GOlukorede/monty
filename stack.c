#include "monty.h"

/**
 * stack - print the top stack
 * @head: the head stack
 * @line_n: the line number
 * Return: Return nothing
 */

void stack(stack_t **head, unsigned int line_n)
{
(void)head;
(void)line_n;
m.stackMode = 0;
}
