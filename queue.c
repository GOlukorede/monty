#include "monty.h"


/**
 * queue - prints the top of a queue
 * @head: the queue head
 * @line_n: the line number
 * Return: Return nothing
 */
void queue(stack_t **head, unsigned int line_n)
{
(void)head;
(void)line_n;
m.stackMode = 1;
}
